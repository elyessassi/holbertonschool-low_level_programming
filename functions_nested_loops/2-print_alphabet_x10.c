#include "main.h"

/**
 * 
 */

void print_alphabet_x10(void)
{
    int x = 0;
    while (x != 10)
    {
        x++
	    for (x = 'a' ; x <= 'z' ; x++)
	    {
		    _putchar(x);
	    }
    _putchar('\n');
    }
}
